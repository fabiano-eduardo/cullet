# __KIT_NAME__ — contexto para agentes

> Arquivo de exemplo do payload. Tudo neste diretório (`files/`) é copiado para o
> placement declarado em `meta.json` quando alguém roda `npx cullet fc __KIT_NAME__`.
> Substitua este conteúdo pelo que o kit realmente entrega (instruções, hooks,
> settings, scripts).

## O que este kit adiciona

Descreva aqui o que o harness/tooling passa a oferecer ao projeto consumidor.
