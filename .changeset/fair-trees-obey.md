---
---

chore: remove stale duplicate files (kits/kit-spec.schema.json and
packages/cli/src/cli/index.js). Internal cleanup only — the published cullet
tarball and build entry are unchanged, so no version bump is needed.
