---
---

fix(scripts): coleta dynamic imports com import attributes (`import("x", { with: ... })`) e parseia arquivos `.tsx` com `ScriptKind.TSX` em `source-analysis.mjs`. Repo tooling only — no published package changes, so this is an intentionally empty changeset (no version bump / no release).
