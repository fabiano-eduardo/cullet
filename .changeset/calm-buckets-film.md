---
---

chore(cli): align the workspace/package metadata and CLI help text with
cullet's dual delivery model: importable library kits plus copy-first tooling
kits via the CLI. This is metadata and copy only, so it can ship with the next
planned release instead of forcing a standalone version bump.